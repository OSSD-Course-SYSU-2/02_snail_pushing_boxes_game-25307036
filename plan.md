# 蜗牛推箱子页面美感提升计划

## 目标

将当前界面从“功能完整的课程项目页面”提升为“可发布的精致休闲益智游戏界面”。本计划只关注页面美感，不改玩法规则、关卡数据、存档逻辑、键盘逻辑和音频逻辑。

整体视觉方向：森林、荷叶、木箱、蜗牛、暖光、童话感、轻量拟物。界面需要看起来柔和、统一、干净、有层次，避免大量零散颜色和过重的信息块。

## 修改范围

主要修改 `entry/src/main/ets/pages/Index.ets` 中的页面样式、布局间距、颜色、圆角、阴影、棋盘视觉、按钮样式和完成弹层视觉。

可以新增视觉常量文件，例如：

```text
entry/src/main/ets/theme/VisualTheme.ets
```

不修改以下内容：

```text
entry/src/main/ets/utils/SokobanLogic.ets
entry/src/main/ets/utils/KeyboardMapping.ets
entry/src/main/ets/data/SnailLevels.ets
```

## 一、统一视觉主题

### 1. 建立统一配色

当前页面中颜色分散在大量组件里，建议提取为统一主题色。推荐使用以下视觉体系：

- 深林背景：`#0E241A`
- 主卡片深绿：`#173629`
- 次级卡片绿：`#214536`
- 暖纸卡片：`#F4E6CB`
- 纸卡片阴影色：`#00000033`
- 主按钮金色：`#F2C762`
- 主按钮深边框：`#8D5A19`
- 成功绿色：`#57A66A`
- 警告橙色：`#D08A3E`
- 危险红棕：`#B45A43`
- 标题奶白：`#FFF7E8`
- 正文米灰：`#E5D8BE`
- 深色文字：`#2B2117`

Codex 执行要求：

- 将重复出现的颜色抽到 `VisualTheme.ets`。
- 所有卡片、按钮、标签、棋盘格、提示框都使用主题色。
- 减少蓝色、灰色、红色等跳出的孤立颜色，让整体保持森林暖调。
- 只保留金色作为主行动色，绿色作为背景与状态色，红棕色作为危险提示色。

### 2. 建立统一圆角和阴影

建议使用以下统一规格：

- 大卡片圆角：`28`
- 普通卡片圆角：`22`
- 小卡片圆角：`16`
- 标签圆角：`999`
- 主按钮圆角：`20`
- 棋盘外框圆角：`28`
- 棋盘格圆角：`8` 到 `12`

阴影建议：

- 大卡片：`radius: 20, offsetY: 8, color: '#00000033'`
- 普通按钮：`radius: 10, offsetY: 4, color: '#0000002E'`
- 主按钮：`radius: 18, offsetY: 6, color: '#00000044'`
- 完成弹层：`radius: 28, offsetY: 10, color: '#00000066'`

所有阴影统一向下，避免上下左右随意投影。

## 二、启动页美化

当前启动页只有背景图和“开始游戏”按钮，视觉记忆点不够强。建议改成完整的游戏封面页。

### 1. 背景加暗角和渐变遮罩

在 `startPage()` 的背景图上方增加一层半透明遮罩：

- 顶部轻微深绿色遮罩。
- 底部加深，让按钮区域更清晰。
- 中央保留图片细节，避免整页变暗。

视觉目标：背景图仍然可见，但按钮和标题不会被背景干扰。

### 2. 增加标题区

在启动页中下方加入游戏标题：

```text
蜗牛推箱子
慢慢推，也能抵达终点
```

标题样式：

- 主标题字号：`40` 到 `48`
- 字重：`Bolder`
- 颜色：`#FFF7E8`
- 增加轻微文字阴影
- 副标题字号：`15` 到 `17`
- 副标题颜色：`#E9D9B8`

标题和按钮保持垂直排列，整体居中，不要只在底部孤立一个按钮。

### 3. 优化开始按钮

按钮从普通矩形升级为“发布级主按钮”：

- 宽度：紧凑屏幕 `72%`，宽屏 `360`
- 高度：`60`
- 背景：金色
- 字色：深棕
- 圆角：`22`
- 边框：深金棕
- 阴影：更柔和但明显
- 按钮文字可改为：`开始冒险`

增加按钮下方小提示：

```text
支持关卡选择、通关评级和本地记录
```

此提示仅作视觉氛围，不影响功能。

## 三、首页美化

首页包含 `homeHeroCard()`、`levelOverviewCard()` 和 `featureCard()`。目前信息完整，但卡片之间的视觉层级还可以更强。

### 1. 首页背景增加层次

把纯色背景升级为深绿色渐变或多层色块感：

- 页面底色：深林绿
- 顶部可加入轻微暖光区域
- 卡片之间保持 20 到 24 的间距
- 页面左右留白在宽屏下更宽，避免内容贴边

视觉目标：首页像一个完整游戏大厅，而不是普通表单页面。

### 2. 强化英雄卡片

`homeHeroCard()` 是首页视觉中心，需要最精致。

建议：

- 背景使用比页面更亮的森林绿。
- 加大圆角到 `30`。
- 增加柔和阴影。
- 标题字号宽屏提升到 `46`，紧凑屏幕保持 `34` 到 `38`。
- 标题下方文案宽度控制在 `70%` 左右，避免长句横跨整行。
- 指标卡片增加顶部高光或底部金色细条。

英雄卡片可加入装饰元素：

- 右上角半透明荷叶圆形图案。
- 左下角小蜗牛剪影或浅色圆点纹理。
- 不要使用过多图案，装饰透明度控制在 `0.08` 到 `0.18`。

### 3. 优化标签 pill

当前 pill 颜色较杂，建议统一为三类：

- 金棕标签：版本、推荐、标杆
- 绿色标签：已完成、进行中、可挑战
- 红棕标签：死角预警、挑战难度

pill 样式：

- 字号：`12`
- 左右 padding：`12`
- 上下 padding：`7`
- 背景色统一来自主题色
- 加轻微内阴影或边框

### 4. 优化关卡卡片

`levelCard()` 需要从“列表项”变成“游戏关卡卡”。

建议：

- 卡片背景使用暖纸色。
- 当前关卡使用更亮的金色边框。
- 已通关关卡右上角显示星星视觉元素。
- 未挑战关卡保持低饱和度。
- 鼠标或点击反馈使用轻微缩放或颜色变亮。
- 卡片内部信息块 `metaCard()` 保持统一小纸片风格。

关卡卡片的视觉重点顺序：

1. 关卡名称
2. 当前状态
3. 星级或纪录
4. 提示文字

不要让所有文字颜色都一样重。

### 5. 系统特色卡片弱化

`featureCard()` 目前是功能说明，视觉上不应抢过英雄卡片和关卡入口。

建议：

- 背景稍暗。
- 三个 feature item 使用统一深绿小卡片。
- 每个标题前可以加一个小圆点或小图标位。
- 正文字号略小，颜色降低对比。

视觉目标：它是辅助信息，不是主视觉。

## 四、游玩页整体美化

游玩页包含左侧控制、中央棋盘、右侧路线总览。当前结构清楚，但视觉上偏“面板拼接”。建议突出棋盘，让它成为绝对主角。

### 1. 强化棋盘中心感

在宽屏布局中：

- 中央棋盘列保持最大视觉权重。
- 左侧控制卡和右侧关卡列表降低饱和度。
- 棋盘卡片增加更强的阴影和边框。
- 棋盘标题行与棋盘主体之间增加空间。

视觉比例建议：

- 左侧控制区：`28%` 到 `30%`
- 中央棋盘区：自适应最大
- 右侧路线区：`26%` 到 `28%`

当前布局已经接近，只需要在视觉重量上让棋盘更突出。

### 2. 游玩页头部更像游戏 HUD

`playHeader()` 建议改成“顶部状态栏”视觉：

- 背景增加半透明深绿卡片。
- 当前关卡标题更大。
- 难度、目标、最好成绩等信息用统一标签排布。
- “返回总览”按钮弱化，不要比当前关卡标题更抢眼。

建议标题样式：

- 宽屏字号：`34`
- 紧凑字号：`28`
- 颜色：奶白
- 下方副标题：米灰

## 五、棋盘美化

棋盘是最重要的页面美感来源，需要重点打磨。

### 1. 棋盘外框升级

`boardCard()` 中的棋盘容器建议使用三层结构：

1. 外层深绿卡片
2. 中层木质或泥土色边框
3. 内层棋盘网格

视觉建议：

- 外层背景：`#173126`
- 中层背景：`#7B5A35` 或 `#D3B17A`
- 内层背景：暖沙色
- 外层圆角：`28`
- 内层圆角：`22`
- 外层阴影更明显

### 2. 棋盘格更细腻

当前格子主要依赖纯色切换。建议：

- 普通地面使用浅米色棋盘交错。
- 目标格使用荷叶绿色，而不是只靠浅黄区分。
- 墙体使用苔石绿，边框更深。
- 空白区域保持透明，不要产生噪点。

推荐颜色：

- 普通地面 A：`#F2E1BD`
- 普通地面 B：`#E8D4A8`
- 目标格：`#CDE49A`
- 墙体：`#637A4A`
- 墙体边框：`#3E5133`
- 空白透明或页面背景色

### 3. 棋盘格添加轻微立体感

每个可见格子：

- 添加 `border`。
- 上边缘颜色略亮。
- 下边缘颜色略暗。
- 墙体圆角比地面大一点。

不要使用过重阴影，否则棋盘会显脏。

### 4. 目标点改成荷叶风格

`goalToken()` 现在已经有荷叶感，但可以更精致：

- 荷叶主形状更扁，颜色更清新。
- 加一个缺口或浅色叶脉。
- 中心点用金色小圆点表示目标。
- 透明度不要太低，确保玩家能看清目标。

### 5. 木箱改成木 crate 风格

`boxToken()` 建议突出木质质感：

- 主体使用暖棕。
- 加横向高光条。
- 加交叉木条。
- 在目标上时变成金棕高亮。
- 死角时加入红色外发光，而不是只把箱子改红。

视觉目标：正常木箱、已到目标木箱、死角木箱三者一眼可分辨，但仍保持同一美术风格。

### 6. 蜗牛角色更可爱

`snailToken()` 是游戏记忆点，建议进一步美化：

- 壳体增加螺旋线视觉。
- 身体颜色更暖。
- 眼睛更明显。
- 触角更细。
- 角色整体略大于箱子，但不要遮挡格子。
- 可加非常轻微的投影，让蜗牛浮在地面上。

不要把蜗牛画得过复杂，必须在小尺寸格子里依然清楚。

## 六、按钮和控制面板美化

### 1. 统一按钮风格

当前按钮颜色和层级较多。建议统一为三类：

- 主按钮：金色，深棕文字，用于开始、继续、下一关。
- 次按钮：森林绿，白色文字，用于返回、推荐关卡。
- 危险按钮：红棕色，白色文字，用于重开。

所有按钮：

- 圆角统一。
- 阴影统一。
- 边框统一。
- 禁用状态使用透明度降低，不要改变成新的颜色体系。

### 2. 方向按钮更像游戏控制器

`directionPad()` 中方向按钮建议从普通文字按钮改成更强的游戏控制器视觉：

```text
    ▲
◀  ▼  ▶
```

样式：

- 按钮大小：`56` 到 `64`
- 圆角：`18`
- 背景：深绿或苔绿
- 箭头：奶白
- 按下时略微缩小
- 阴影更短，像实体按键

如果不方便使用图标，可以直接用字符 `▲`、`▼`、`◀`、`▶`。

### 3. 控制卡片改成暖纸面板

`controlCard()` 和 `ruleCard()` 建议保留暖纸色，但提升质感：

- 背景：`#F4E6CB`
- 边框：`#D9BE8A`
- 圆角：`24`
- 阴影：轻微下投影
- 标题颜色：深棕
- 信息行用浅色分割线或小卡片承载

视觉目标：像放在森林里的纸质任务卡。

### 4. 状态提示框更有情绪

`statusText` 现在已经根据状态有不同颜色，建议改成更精致的提示条：

- 普通：浅米色
- 成功：淡绿
- 警告：浅橙
- 死角：浅红

每种提示增加左侧色条：

- 普通：绿色
- 成功：亮绿
- 警告：橙色
- 死角：红棕

这样不用增加文字，也能提升视觉识别。

## 七、完成弹层美化

`clearOverlay()` 是通关后的高光时刻，需要更有发布感。

### 1. 弹层背景升级

建议使用深绿半透明背景：

- 背景色：`#163927F2`
- 边框：金绿色半透明
- 圆角：`26`
- 阴影：更大更柔和
- 弹层内部增加金色高光线

### 2. 星级展示更突出

星星样式：

- 已获得星星：亮金色
- 未获得星星：暗绿色或灰金色
- 字号：`30` 到 `34`
- 星星之间间距：`8`
- 通关动画中星星依次出现

### 3. 通关按钮层级清晰

- “下一关”使用主按钮金色或成功绿色。
- “重玩本关”使用次按钮棕色。
- 两个按钮间距加大。
- 按钮高度保持 `46` 到 `50`。

### 4. 增加庆祝氛围

可以在弹层周围添加轻量装饰：

- 小金点
- 半透明光晕
- 顶部小荷叶图案

不要添加复杂粒子，避免影响性能和代码复杂度。

## 八、移动端视觉优化

当前紧凑布局已经存在，但需要让移动端看起来不像桌面版压缩。

### 1. 移动端首页

- 英雄卡片 padding 使用 `20`。
- 标题不要超过两行。
- 指标卡片两列排列。
- 主要按钮垂直排列，宽度 `100%`。
- 关卡卡片之间间距加到 `12`。

### 2. 移动端游玩页

- 棋盘放在控制卡片之前，优先展示核心内容。
- 方向控制放在棋盘下方。
- 规则卡片可以放最后，视觉上降低优先级。
- 页面 padding 保持 `14` 到 `16`，避免贴边。

### 3. 移动端棋盘

- 棋盘格最小尺寸不低于 `24`。
- 角色和箱子在小格子里仍可识别。
- 棋盘外框 padding 在小屏减少，避免挤压格子。

## 九、Codex 执行步骤

### 步骤 1：新增视觉主题常量

新增：

```text
entry/src/main/ets/theme/VisualTheme.ets
```

包含：

- `Colors`
- `Radius`
- `Spacing`
- `Shadow`
- `FontSize`

如果项目结构不适合新增文件，也可以先在 `Index.ets` 顶部集中定义常量，但不要继续散落硬编码颜色。

### 步骤 2：替换全局背景和卡片样式

修改：

- `build()` 中 Scroll 背景
- `homeHeroCard()`
- `levelOverviewCard()`
- `featureCard()`
- `controlCard()`
- `ruleCard()`
- `boardCard()`
- `levelRailCard()`

目标：所有卡片圆角、阴影、背景色统一。

### 步骤 3：重做启动页视觉

修改：

- `startPage()`

加入：

- 背景遮罩
- 游戏标题
- 副标题
- 更精致的开始按钮
- 小提示文字

### 步骤 4：优化首页视觉层级

修改：

- `homeHeroCard()`
- `metricCard()`
- `pill()`
- `levelCard()`
- `metaCard()`
- `featureItem()`

目标：英雄卡片最醒目，关卡卡片更像游戏卡牌，系统特色弱化为辅助说明。

### 步骤 5：强化棋盘视觉

修改：

- `boardCard()`
- `boardCell()`
- `goalToken()`
- `boxToken()`
- `snailToken()`
- `cellBackgroundColor()`
- `cellBorderColor()`

目标：棋盘有外框层次，地面、墙体、目标、木箱和蜗牛都更精致。

### 步骤 6：统一按钮和方向键

修改：

- `heroButton()`
- `actionButton()`
- `overlayButton()`
- `directionButton()`
- `directionPad()`

目标：按钮统一圆角、阴影、边框和颜色；方向键改成箭头视觉。

### 步骤 7：美化完成弹层

修改：

- `clearOverlay()`
- `clearOverlayOpacity()`
- `clearOverlayScale()`
- `clearOverlayTranslateY()`
- `clearOverlayStarOpacity()`

目标：通关时有强烈奖励感，但不夸张、不杂乱。

### 步骤 8：微调移动端样式

修改：

- `isCompactLayout()` 相关分支中的 padding、space、width
- `cellSize()` 的紧凑屏计算
- 首页和游玩页的 Column/Row 间距

目标：移动端不是简单压缩，而是有专门的舒适排版。

## 十、验收标准

完成后页面应满足以下视觉标准：

- 启动页像完整游戏封面，不再只有背景图和按钮。
- 首页第一眼能看到游戏名、进度和主要行动按钮。
- 卡片圆角、阴影、颜色体系统一。
- 棋盘是游玩页视觉中心，明显比侧边面板更突出。
- 蜗牛、木箱、目标、墙体有统一的森林童话风格。
- 按钮看起来可点击，主次层级清楚。
- 通关弹层有奖励感，星级展示突出。
- 移动端没有拥挤、贴边或按钮过小的问题。
- 页面整体不出现突兀的孤立颜色。
- 不改变任何玩法逻辑、关卡逻辑、存档逻辑和音频逻辑。

## 十一、禁止事项

Codex 不要做以下事情：

- 不要改关卡数据。
- 不要改推箱子规则。
- 不要改存档结构。
- 不要删除现有功能。
- 不要新增复杂页面流程。
- 不要加入大量新文字说明。
- 不要把页面改成赛博、极简黑白、科幻或写实风。
- 不要使用过多高饱和颜色。
- 不要让装饰元素遮挡棋盘或按钮。
- 不要为了美化牺牲棋盘可读性。

