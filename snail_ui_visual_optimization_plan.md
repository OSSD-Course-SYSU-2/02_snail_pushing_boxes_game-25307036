# 《蜗牛推箱子》页面美感二次优化设计方案

## 1\. 优化目标

本方案只关注页面美感，不改玩法逻辑、不改关卡数据、不改存档结构、不改音频播放逻辑。

当前页面已经具备完整游戏流程，并且启动页已经有较强的休闲手游封面感。下一步重点不是继续堆功能，而是让首页、关卡列表、系统特色页和游玩页的视觉品质接近启动页的完成度。

最终目标：

* 启动页保持“正式游戏封面”质感。
* 首页从“信息面板”升级为“游戏大厅”。
* 关卡列表从“表格式卡片”升级为“冒险地图卡片”。
* 系统特色页从“大面积空卡片”升级为“轻量展示区”。
* 棋盘页从“代码绘制棋盘”升级为“美术资源棋盘”。
* 所有按钮、卡片、星星、箱子、目标点、蜗牛统一成森林童话风。

## 2\. 当前截图诊断

### 2.1 启动页

启动页已经是当前最接近发布标准的一页。背景有天空、草地、石块、木箱、蜗牛和大标题，整体风格明确。

仍有几个可优化点：

* 顶部大标题已经写了“蜗牛推箱子”，底部又出现一次“蜗牛推箱子”，信息重复。
* 底部按钮风格和封面图接近，但仍略像系统按钮，可以进一步做成游戏主按钮。
* 底部说明文字较弱，可以减少存在感，避免抢按钮视觉。
* 启动页可加入少量闪光粒子，强化“开始冒险”的期待感。

优化方向：保留当前封面图主体，只对按钮、文案层级、粒子装饰和底部留白做微调。

### 2.2 首页英雄区

当前首页使用深绿色背景、金色描边、指标卡片和两个大按钮，整体整齐，但有明显“管理面板感”。

主要问题：

* 深绿色区域面积较大，背景偏平，缺少森林环境氛围。
* 金色描边出现过多，所有区域都被框住，视觉有些硬。
* 右上角黄色椭圆装饰语义不清，像临时图形。
* 四个指标卡片虽然整齐，但缺少图标和游戏感。
* 主按钮和次按钮只是颜色不同，层级还不够强。

优化方向：把首页英雄区做成“冒险大厅卡片”，使用美术资源中的装饰、闪光、按钮素材，让它更像游戏主界面，而不是数据面板。

### 2.3 关卡总览

当前关卡卡片信息完整，奶油色卡片和深绿背景对比清晰。

主要问题：

* 每个关卡卡片结构重复，列表下滑时会有疲劳感。
* 目标、标杆、记录三个小块像表格，游戏感不足。
* 星星使用文字符号，和启动页的美术质感不统一。
* 关卡名称使用英文，视觉上略跳出中文游戏主题。
* 当前通关状态和按钮状态没有足够的奖励感。

优化方向：关卡卡片使用羊皮纸面板资源，星星换成美术资源，目标图标使用荷叶或木箱，记录区域做成小徽章。

### 2.4 系统特色

系统特色页当前问题最明显。

主要问题：

* 每个特色卡片高度很大，但内容很少，出现大量空白。
* 卡片只靠文字说明，缺少图标和装饰。
* 该区域不需要如此强的存在感，容易拉长页面并降低精致度。
* 三张大卡片重复使用金色边框，和页面其他区域竞争注意力。

优化方向：系统特色应改成“轻量展示区”，高度降低，加入小图标、小圆点、短文案，不再使用大空卡片。

### 2.5 游玩页棋盘

游玩页的棋盘布局清晰，已经能看出墙、地面、木箱、目标和蜗牛。

主要问题：

* 墙体和地面仍然像代码画出来的圆角块，缺少真实材质。
* 蜗牛和木箱尺寸偏小，缩小时辨识度不够。
* 目标点目前像小胶囊，和“荷叶终点”的设定不够一致。
* 方向键区域的蓝色左右按钮破坏了整体森林配色。
* 棋盘外框较厚，但质感偏普通，没有木框或石框感觉。
* “棋盘旁控制”模块文字偏功能说明，不像游戏 UI。

优化方向：用资源包中的棋盘素材替换代码绘制图形，让棋盘成为整页视觉核心。

## 3\. 美术资源使用规划

项目目录中添加了美术资源：

```entry/src/main/resources/base/media/
```

建议保留以下文件名，不要随意改名：

|文件|用途|
|-|-|
|`snail\_player.png`|蜗牛角色|
|`wood\_box.png`|普通木箱|
|`wood\_box\_deadlock.png`|死角木箱|
|`lily\_goal.png`|荷叶目标点|
|`moss\_wall\_tile.png`|苔石墙块|
|`forest\_floor\_tile.png`|草地地面|
|`gold\_star.png`|通关星星|
|`ui\_button\_primary.png`|主按钮|
|`ui\_button\_green.png`|次按钮|
|`card\_panel\_parchment.png`|羊皮纸卡片|
|`start\_page\_decoration.png`|启动页装饰|
|`sparkles.png`|闪光粒子|

ArkUI 中按下面方式引用：

```ts
Image($r('app.media.snail\_player'))
Image($r('app.media.wood\_box'))
Image($r('app.media.lily\_goal'))
Image($r('app.media.gold\_star'))
```

## 4\. 全局视觉规范

### 4.1 色彩

继续使用森林童话风，但减少过多金色描边。

推荐主色：

```text
深森林背景：#0B2418
主卡片绿：#123724
次级卡片绿：#1B4A34
奶油纸卡：#F5E8C9
暖金按钮：#F4C85A
木箱棕色：#8A5528
柔和白字：#FFF7E6
弱化说明字：#D8CFB6
```

使用规则：

* 深绿负责背景和大容器。
* 奶油色负责关卡卡片、规则卡片、提示卡片。
* 金色只用于主按钮、星星、少量高亮线条。
* 不要到处画金色边框，避免页面显得廉价。
* 每个页面最多保留一个最亮的视觉焦点。

### 4.2 圆角

当前圆角较统一，但可以更游戏化。

建议：

* 大容器圆角：28 到 34。
* 普通卡片圆角：20 到 24。
* 小标签圆角：999。
* 棋盘格圆角：8 到 12。
* 按钮圆角：18 到 24。

### 4.3 阴影

增加柔和阴影，让页面更有层次。

建议：

* 大卡片阴影：向下偏移 8 到 12。
* 按钮阴影：向下偏移 4 到 6。
* 棋盘阴影：向下偏移 10 到 16。
* 不要使用过黑的阴影，建议透明黑或深绿色阴影。

### 4.4 文字层级

当前文字信息较多，需要更明确层级。

建议：

* 游戏标题：36 到 44，超粗，暖白色。
* 页面标题：26 到 30，粗体。
* 卡片标题：18 到 22，粗体。
* 正文说明：14 到 16，行高 22 到 26。
* 辅助说明：12 到 13，降低透明度。
* 数字指标：22 到 28，粗体，颜色更亮。

## 5\. 页面级优化方案

## 5.1 启动页优化

启动页保留当前背景图，不要大改构图。

需要改的内容：

1. 删除或弱化底部重复标题。

当前底部再次显示“蜗牛推箱子”，建议改成一句更轻的副标语：

```text
慢慢推，也能抵达终点
```

这句可以放在按钮上方，字号 22 到 26，颜色暖白。

2. 主按钮使用 `ui\_button\_primary.png`。

按钮不再只用纯色背景。用图片资源作为底层，再叠加文字“开始冒险”。

视觉要求：

* 宽度约 72%。
* 高度 64 到 72。
* 文字颜色 #3A2407。
* 字号 22 到 24。
* 字重加粗。
* 按下时轻微缩小到 0.96。
3. 添加 `sparkles.png`。

在标题附近和按钮附近低透明度叠加闪光粒子。

建议：

* 标题周围透明度 0.16。
* 按钮上方透明度 0.10。
* 不要满屏铺满。
4. 添加 `start\_page\_decoration.png`。

可以放在底部左右角或标题边缘，作为叶子装饰。

注意：装饰只做氛围，不要遮挡蜗牛和木箱。

## 5.2 首页英雄区优化

首页目前需要从“数据卡片”改成“冒险大厅”。

### 结构调整

英雄卡片仍保留以下内容：

* 三个小标签。
* 游戏标题。
* 一段说明文字。
* 四个指标。
* 主按钮和次按钮。

但视觉上要重新组织。

建议布局：

```text
顶部：小标签 + 叶子装饰
中部：大标题 + 简短说明
中下：四个指标卡片
底部：主按钮 + 次按钮
背景：深绿卡片 + 轻微闪光 + 半透明叶子装饰
```

### 具体改法

1. 删除右上角黄色椭圆。

当前黄色椭圆意义不明确。替换为 `start\_page\_decoration.png` 的叶子装饰，透明度 0.35，放在右上角。

2. 指标卡片加入小图标。

建议：

* “已完成”旁边使用 `lily\_goal.png` 小图标。
* “总评级”旁边使用 `gold\_star.png` 小图标。
* “推荐关卡”旁边使用小荷叶或木牌符号。
* “当前记录”旁边使用小脚印或木箱图标。

如果没有额外图标，就用现有资源缩小显示。

3. 指标卡片减少描边。

不要所有卡片都用金色边框。可改成：

* 默认无边框。
* 顶部一条金色短线。
* 当前推荐卡片才用细金边。
4. 主按钮使用 `ui\_button\_primary.png`。

“继续冒险”是页面第一行动，必须最亮。

5. 次按钮使用 `ui\_button\_green.png`。

“挑战推荐关卡”保持深绿，但需要有边缘高光，不要只是纯色按钮。

## 5.3 关卡总览优化

关卡总览是第二个重点区域。它决定页面是否像游戏，而不是像课程作业。

### 关卡卡片

使用 `card\_panel\_parchment.png` 做卡片背景。

如果 ArkUI 不能直接做图片背景，就使用 `Stack`：

```ts
Stack() {
  Image($r('app.media.card\_panel\_parchment'))
    .width('100%')
    .height('100%')
    .objectFit(ImageFit.Fill)

  Column() {
    // 原关卡内容
  }
}
```

### 关卡标题

建议中文优先，英文降级为副标题。

当前：

```text
Garden Gate
Mossy Bend
Twin Ponds
```

建议显示方式：

```text
第 1 关 · 花园门口
Garden Gate
```

如果关卡数据里暂时没有中文名，可以先保持英文，但要降低英文的锐度：

* 英文字体不要太黑。
* 颜色用 #2D241A。
* 第几关用更小、更淡的文字。

### 目标、标杆、记录

当前三个小块像表单。建议改成“徽章式信息”。

目标：

* 左侧放 `wood\_box.png` 小图标。
* 文案显示“1 箱”。

标杆：

* 左侧放小脚印或用简单符号。
* 文案显示“3 步”。

记录：

* 左侧放 `gold\_star.png`。
* 文案显示“3 星”。

### 星星显示

所有星星用 `gold\_star.png`，不要用文字“★”。

建议：

* 已获得星星：正常亮度。
* 未获得星星：透明度 0.25 或灰金色。
* 星星区域靠右下角，形成奖励感。

### 卡片当前状态

建议：

* 当前关卡：卡片外层有柔和金光。
* 已通关：右上角绿色章。
* 未通关：右上角木牌章。
* 推荐关卡：添加小叶子装饰或微弱闪光。

## 5.4 系统特色区优化

系统特色不应占用大量页面高度。

### 当前问题处理

把现在的三张大空卡片改成三条横向或纵向的小介绍卡。

移动端建议：

```text
稳定规则    一句话说明
局内反馈    一句话说明
发布流程    一句话说明
```

每条高度控制在 86 到 110，不再使用 200 以上的大卡。

### 图标使用

每条左侧加一个小圆图标：

* 稳定规则：使用 `moss\_wall\_tile.png` 缩小。
* 局内反馈：使用 `snail\_player.png` 缩小。
* 发布流程：使用 `gold\_star.png` 缩小。

### 视觉要求

* 背景用深绿半透明卡片。
* 边框只保留非常淡的金线。
* 文案改短，避免说明书感。
* 整个模块不要比关卡列表更抢眼。

## 5.5 游玩页棋盘优化

游玩页是核心，需要优先使用美术资源。

### 棋盘格替换

在 `boardCell(row, col)` 中，先放底图，再叠加目标、箱子和蜗牛。

墙体：

```ts
Image($r('app.media.moss\_wall\_tile'))
```

地面：

```ts
Image($r('app.media.forest\_floor\_tile'))
```

目标点：

```ts
Image($r('app.media.lily\_goal'))
```

木箱：

```ts
Image($r('app.media.wood\_box'))
```

死角木箱：

```ts
Image($r('app.media.wood\_box\_deadlock'))
```

蜗牛：

```ts
Image($r('app.media.snail\_player'))
```

### 棋盘层级

每格建议层级：

```text
第 1 层：地面或墙体
第 2 层：目标点荷叶
第 3 层：木箱
第 4 层：蜗牛
第 5 层：死角警告光效
```

### 尺寸建议

假设 `size = this.cellSize()`：

```text
墙体图片：size × size
地面图片：size × size
荷叶目标：size × 0.76
木箱：size × 0.86
蜗牛：size × 0.92
死角木箱：size × 0.88
```

注意：蜗牛不要太小。当前棋盘里蜗牛识别度偏弱，建议比木箱略大。

### 棋盘外框

当前棋盘外框已经有厚度，但缺少材质。

建议改成：

* 外层深木色边框。
* 中层浅木色或泥土色边框。
* 内层棋盘格。
* 外层加柔和阴影。

颜色建议：

```text
外框：#8A5A2C
内框：#C99B55
阴影：#00000044
```

### 棋盘标题

当前“棋盘”标题在截图中靠左且略显紧。建议：

* 标题行增加左侧小荷叶图标。
* “竖版地图”标签变成小木牌。
* 棋盘标题和棋盘主体之间留 12 到 16 间距。

## 5.6 方向键区域优化

当前方向键区域有明显风格问题，尤其左右按钮是蓝色图标，和森林童话风不一致。

### 改法

所有方向键都统一使用 `ui\_button\_green.png` 或深绿色圆角按钮。

按钮中只放白色箭头：

```text
▲
◀  ▼  ▶
```

不要出现蓝色小方块。

### 尺寸

移动端建议：

```text
方向键宽度：64 到 72
方向键高度：58 到 64
圆角：20
间距：10 到 12
```

### 质感

方向键应像“软软的绿色游戏按钮”，不是系统控件。

建议：

* 按钮背景深绿。
* 边缘金线很细。
* 顶部有一点高光。
* 按下时缩小到 0.96。

## 5.7 控制面板与提示框优化

控制面板目前功能清晰，但视觉上偏普通。

建议：

1. 控制面板使用奶油纸卡或深绿卡。

如果在棋盘下方，建议用深绿卡，保持整体沉浸。

2. 状态提示框用更强的语气色。

普通提示：奶油纸色。
成功提示：浅绿色。
警告提示：浅橙色。
死角提示：浅红棕色。

3. 操作按钮按重要性分层。
* 下一关：主绿色或金色。
* 撤销：木色。
* 重开：红棕色。
* 返回总览：深绿次级按钮。
4. 按钮统一圆角和阴影。

不要不同位置的按钮看起来像不同系统的控件。

## 5.8 通关弹层优化

通关弹层应成为奖励感最强的瞬间。

### 背景

使用 `card\_panel\_parchment.png` 作为弹层底图，外面加深色半透明遮罩。

### 星星

使用 `gold\_star.png`，三颗星依次出现。

建议动效：

```text
第 1 颗：0ms 出现
第 2 颗：120ms 出现
第 3 颗：240ms 出现
```

每颗星出现时：

```text
透明度 0 -> 1
缩放 0.75 -> 1.12 -> 1.0
```

### 装饰

在弹层背后放 `sparkles.png`，透明度 0.18。

### 按钮

“下一关”使用 `ui\_button\_primary.png`。
“重玩本关”使用 `ui\_button\_green.png`。

## 6\. Codex 执行步骤

### 第一步：导入资源

将以下文件复制到：

```text
entry/src/main/resources/base/media/
```

复制文件：

```text
snail\_player.png
wood\_box.png
wood\_box\_deadlock.png
lily\_goal.png
moss\_wall\_tile.png
forest\_floor\_tile.png
gold\_star.png
ui\_button\_primary.png
ui\_button\_green.png
card\_panel\_parchment.png
start\_page\_decoration.png
sparkles.png
```

### 第二步：新增视觉常量

在 `Index.ets` 顶部新增颜色、圆角、阴影相关常量，减少硬编码。

建议常量：

```ts
const UI\_BG: string = '#0B2418';
const UI\_CARD: string = '#123724';
const UI\_CARD\_SOFT: string = '#1B4A34';
const UI\_PAPER: string = '#F5E8C9';
const UI\_GOLD: string = '#F4C85A';
const UI\_TEXT: string = '#FFF7E6';
const UI\_TEXT\_MUTED: string = '#D8CFB6';
const UI\_BROWN: string = '#8A5528';
```

### 第三步：替换棋盘素材

重点修改 `boardCell`、`goalToken`、`boxToken`、`snailToken`。

推荐做法：

* `goalToken` 改为 Image 资源。
* `boxToken` 改为 Image 资源。
* `snailToken` 改为 Image 资源。
* 墙体和地面也使用 Image 资源。
* 保留原逻辑判断，不改变移动规则。

### 第四步：替换星星显示

所有文字星星“★”替换成 `gold\_star.png`。

影响区域：

* 关卡卡片评级。
* 通关弹层评级。
* 首页总评级图标。

### 第五步：替换按钮风格

主按钮使用 `ui\_button\_primary.png`。
次按钮使用 `ui\_button\_green.png`。

影响区域：

* 启动页“开始冒险”。
* 首页“继续冒险”。
* 首页“挑战推荐关卡”。
* 通关弹层按钮。
* 方向键按钮。

### 第六步：压缩系统特色区

把系统特色的三张大卡片改成三条小卡片。

要求：

* 不要大面积空白。
* 每条左侧有图标。
* 每条高度不超过 110。
* 文案保持短句。

### 第七步：优化首页装饰

在首页英雄卡片中加入：

* `start\_page\_decoration.png`，右上角。
* `sparkles.png`，低透明度。
* 指标卡片小图标。

删除无意义黄色椭圆。

### 第八步：截图验收并微调

修改完成后必须检查以下页面：

```text
启动页
首页英雄区
关卡总览列表
系统特色区
游玩页棋盘
通关弹层
```

如果没有截图对比，不算完成 UI 美化。

## 7\. 验收标准

### 启动页

合格标准：

* 第一眼像正式休闲手游封面。
* 只有一个最强标题，不重复抢视觉。
* 主按钮像游戏按钮，不像系统按钮。
* 蜗牛、木箱、标题、按钮四者层级清楚。

### 首页

合格标准：

* 不再像后台数据面板。
* 主按钮是最明显的行动点。
* 指标卡片有游戏图标和奖励感。
* 金色描边不过量。
* 背景有轻微森林氛围。

### 关卡列表

合格标准：

* 每张卡片像游戏关卡卡，而不是表单卡。
* 星星使用图片资源。
* 目标、标杆、记录更像徽章。
* 当前关卡和已通关状态一眼可见。

### 系统特色

合格标准：

* 没有大面积空白卡片。
* 视觉存在感低于关卡列表。
* 图标、标题、说明紧凑清晰。

### 棋盘页

合格标准：

* 一眼能分清墙、地面、木箱、目标、蜗牛。
* 木箱和蜗牛是图片资源，不再像代码图形。
* 方向键没有蓝色突兀元素。
* 棋盘是整页视觉中心。
* 棋盘材质和启动页风格统一。

### 通关弹层

合格标准：

* 有明显奖励感。
* 星星是图片资源并有依次出现效果。
* 弹层不遮挡得过重。
* 下一关按钮比重玩按钮更突出。

## 8\. 禁止改动范围

Codex 不要修改以下内容：

* 不改 `SNAIL\_LEVELS` 的关卡布局。
* 不改 `SokobanLogic` 的移动规则。
* 不改存档字段结构。
* 不改音乐文件名。
* 不新增无关页面。
* 不删除已有的核心功能。
* 不把中文文案改成英文。
* 不为了美化牺牲棋盘可读性。

## 9\. 最优先改动清单

如果时间有限，按这个顺序改：

1. 棋盘格、箱子、目标点、蜗牛换成美术资源。
2. 方向键去掉蓝色按钮，统一成绿色游戏按钮。
3. 关卡卡片星星换成 `gold\_star.png`。
4. 首页主按钮换成 `ui\_button\_primary.png`。
5. 删除首页右上角无意义黄色椭圆。
6. 系统特色区改成短小图标卡。
7. 通关弹层换成羊皮纸面板和图片星星。
8. 启动页按钮换成主按钮资源，并弱化重复标题。

## 10\. 推荐的最终观感

最终页面应该像一个轻松、柔和、童话森林主题的手机益智游戏。

视觉关键词：

```text
森林
草地
荷叶
木箱
蜗牛
暖金色
羊皮纸
柔和阴影
轻奖励感
休闲冒险
```

不要做成：

```text
后台面板
课程项目展示页
纯色卡片堆叠
过度金边装饰
密集说明文字
系统默认按钮
```

